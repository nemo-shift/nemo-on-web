# 네모:ON 홈페이지 기획 문서 (Spec History)

---

## [최신] 🛠️ V16.3 아키텍처 업데이트 (2026.03.22)

초기 기획의 컨셉을 바탕으로, 실제 구현 과정에서의 기술적 무결성과 성능 최적화를 위해 다음과 같은 핵심 아키텍처 결정이 완료되었습니다.

### 1. 애니메이션 엔진 단일화 (Total GSAP)

- **Framer Motion 제거**: 프로젝트 전반에서 `framer-motion`을 완전히 제거함.
- **GSAP 통합**: `RotatingText`, `HeroSlogan`, `ShapesStage` 등 모든 하위 요소를 GSAP `useGSAP` 기반의 커스텀 로직으로 재구현하여 엔진을 단입화함.

### 2. 초정밀 모듈 분리 (Modular Layout)

- **Engine Logic 분순**: 비대한 인터랙션 로직을 `GlobalInteractionStage` 본체와 `builders.ts`(타임라인), `utils.ts`(헬퍼)로 분리하여 유지보수성 확보.

---

# 네모:ON 홈페이지 기획 문서

> 최종 업데이트: 2026.03.13

---

## 1. 전체 컨셉

> **홈페이지가 곧 브랜드 경험이다.**
> 설명하지 않는다. 느끼게 한다.
> 방문자가 스크롤하는 동안, 네모:ON이 무엇인지 몸으로 알게 된다.

---

## 2. 사이트맵

```
nemoon.com
├── /                홈 (고객 여정 기반)
├── /about           네모:ON + 단테 + 브랜드 철학
├── /offerings       서비스 허브 (What we do)
│   ├── /studio      브랜딩 + 웹개발
│   └── /lab         자체 실험 레이블 (간단히 틀만)
├── /diagnosis       브랜드 진단
├── /showcase        포트폴리오 (추후 공개)
├── /story           스토리 블로그 (추후 공개)
└── /contact         컨택
```

> 론칭 시 공개: /, /about, /offerings, /offerings/studio, /offerings/lab, /diagnosis, /contact
> 추후 공개: /showcase, /story

---

## 3. 기술 스택

| 분류              | 기술                 | 비고                                                                                   |
| ----------------- | -------------------- | -------------------------------------------------------------------------------------- |
| 프레임워크        | Next.js (App Router) |                                                                                        |
| 언어              | TypeScript           |                                                                                        |
| 스타일            | Tailwind CSS v4      |                                                                                        |
| 스무스 스크롤     | Lenis                | GSAP ScrollTrigger와 궁합 최적. 네이티브 DOM 구조 유지. 디바이스 간 스크롤 경험 표준화 |
| 스크롤 애니메이션 | GSAP ScrollTrigger   | Lenis와 연동. 스크롤 progress 기반 인터랙션 전체 담당                                  |
| 애니메이션        | GSAP                 |                                                                                        |
| 물리 엔진         | Matter.js            | FallingText                                                                            |
| 상태관리          | React Context        |                                                                                        |
| 패키지 매니저     | pnpm                 |                                                                                        |

> **Lenis 선택 이유**: GSAP ScrollTrigger와 조합이 현재 가장 검증된 방식. Locomotive Scroll은 Lenis 기반 래퍼라 중복이고, GSAP ScrollSmoother는 레이아웃 구조 제약이 있음. CSS Scroll-driven Animations는 보조적으로 활용 가능.

---

## 4. 데이터 구조

콘텐츠 데이터와 설정값을 분리해서 관리.

```
src/
├── constants/       # 변하지 않는 설정값 (컬러 토큰, 타이밍값 등)
│   ├── colors.ts
│   └── hero.ts
├── data/            # 화면에 렌더링되는 콘텐츠 데이터
│   ├── home/
│   │   ├── pain.ts        # Pain 포인트 5개
│   │   ├── forwho.ts      # ForWho 타겟 5개
│   │   └── message.ts     # 메시지 섹션 카피
│   ├── about.ts
│   └── index.ts           # barrel export
└── types/           # 타입 정의
```

| 폴더         | 내용                     | 예시                               |
| ------------ | ------------------------ | ---------------------------------- |
| `constants/` | 코드가 참조하는 설정값   | 컬러 토큰, 타이밍, 애니메이션 수치 |
| `data/`      | 화면에 렌더링되는 콘텐츠 | 카피, 타겟 정보, Pain 포인트       |

---

## 5. 디자인 토큰 (전역 시스템)

> 컬러, 폰트, 그리드는 전역 토큰으로 관리. 각 섹션은 토큰을 참조만 함.
> 히어로 OFF/ON 동적 컬러는 히어로 섹션 전용 — 전역 토큰과 별도.

### 5-1. 브랜드 컬러 시스템

| 역할                   | 토큰           | 색상                                             |
| ---------------------- | -------------- | ------------------------------------------------ |
| 브랜드 틸              | `--brand`      | `#0891b2`                                        |
| 브랜드 오렌지          | `--accent`     | `#E8734A`                                        |
| 다크 배경 (히어로 OFF) | `--bg-dark`    | `#0a0a0a`                                        |
| 섹션 다크 배경         | —              | `#0D1A1F`                                        |
| 라이트 배경            | `--bg-cream`   | `#f7f1e9` (개발 시 `#faf7f2`와 눈으로 최종 결정) |
| 크림 텍스트/요소       | `--text-light` | `#f0ebe3`                                        |
| 다크 텍스트            | `--text-dark`  | `#0d1a1f`                                        |

**배경색 인터폴레이션 (Scroll-driven Color Interpolation)**

섹션 전환 구간에서 배경색이 스크롤 progress에 따라 서서히 전환되는 방식.
네모 오브젝트 모핑과 별개로 배경색 자체를 부드럽게 변화시킴.

구현: GSAP ScrollTrigger `onUpdate` 콜백에서 두 색상을 lerp(선형보간)하여 CSS 변수(`--bg`)에 실시간 주입.

```
scrollProgress 0 → 1
bgColor = lerp(startColor, endColor, progress)
document.documentElement.style.setProperty('--bg', bgColor)
```

| 구간                   | 시작 색   | 끝 색     |
| ---------------------- | --------- | --------- |
| Pain & 공명 → 메시지   | `#0D1A1F` | `#f7f1e9` |
| ForWho → 브랜드 스토리 | `#f7f1e9` | `#0891b2` |

### 5-2. 폰트 시스템

| 구분              | 폰트                | 용도                        |
| ----------------- | ------------------- | --------------------------- |
| **기본 (KR)**     | SUIT Variable       | 한글 전반 (로컬 woff2)      |
| **기본 (EN)**     | DM Sans             | 영문 본문/UI                |
|                   | Bebas Neue          | 영문 빅타이포/헤딩          |
| 터미널            | DM Mono             | 모노/터미널 (CTA 섹션 전용) |
| **타이틀 (네모)** | 이사만룬체 Light    | 로고 전용 (로컬 woff2)      |
| **타이틀 (ON)**   | Gmarket Sans Medium | 로고 전용 (로컬 woff2)      |

> 폰트별 섹션 적용 기준 및 사이즈 체계 미정 (PENDING)

### 5-3. 그리드 시스템

**기본 원칙**: 배경/이미지는 풀블리드(Full-bleed), 콘텐츠는 에디토리얼 그리드

**에디토리얼 그리드 (Editorial Grid)**

잡지/책 같은 인쇄 매체의 정교한 레이아웃 설계 방식.

| 개념                 | 설명                                                          |
| -------------------- | ------------------------------------------------------------- |
| 모듈(Module)         | 그리드의 가장 작은 단위 사각형. 네모결의 'Nemo' 그 자체.      |
| 거터(Gutter)         | 모듈 사이 간격. 좁으면 긴장감, 넓으면 여백의 미.              |
| 플로우라인(Flowline) | 시선이 수평으로 이동하는 기준선. 유저의 시선 흐름(결)을 유도. |
| 마진(Margin)         | 콘텐츠가 들어가지 않는 바깥쪽 여백. 브랜드 무드를 결정.       |

> 전체 섹션/페이지 모두 풀블리드. PC 기준 컬럼 수 미정 (PENDING)

### 5-4. 레이어 계층 시스템 (Z-index)

| 레벨           | 요소                  | 설명                                                  |
| -------------- | --------------------- | ----------------------------------------------------- |
| **LV 4 (Top)** | 전역 헤더 & 햄버거    | 모든 요소 위에 최상위 고정                            |
| **LV 3**       | JourneyLogo (Text)    | 헤더 위치에 안착하여 변해가는 로고 텍스트             |
| **LV 2**       | Pain 섹션 테두리 네모 | 배경 위에서 내용(페인포인트 등)을 담는 Shared Element |
| **LV 1**       | 배경 확장 네모        | 히어로 [결] 뒤 박스가 확장되어 생성된 전체 배경       |

---

## 6. 반응형 정책

**레이아웃** — 뷰포트 너비 기준으로 분기
**인터랙션** — 입력장치 기준으로 분기 (레이아웃과 독립)

**레이아웃 breakpoint**

| breakpoint | 범위             |
| ---------- | ---------------- |
| PC         | `≥ 1024px`       |
| 태블릿     | `768px ~ 1023px` |
| 모바일     | `< 768px`        |

> 태블릿 가로(≥ 1024px)는 PC 레이아웃으로 자연스럽게 처리됨.

| 환경   | 정책                                                               |
| ------ | ------------------------------------------------------------------ |
| PC     | 여유 있는 화이트스페이스, 뷰포트 비례(`vw`, `vh`) 기반 간격.       |
| 태블릿 | 섹션별로 PC 또는 모바일 레이아웃 중 명시. 기본은 PC 레이아웃 따름. |
| 모바일 | 수직형 배치, 터치 고려 간격 확대. 세로 스택형 기본.                |

**인터랙션 분기**

| 조건        | 판단 기준         | 적용                                   |
| ----------- | ----------------- | -------------------------------------- |
| 마우스 있음 | `pointer: fine`   | 커스텀 커서 활성, 호버 인터랙션 활성   |
| 터치만 있음 | `pointer: coarse` | 커스텀 커서 비활성, 터치 인터랙션 활성 |

> 레이아웃과 인터랙션은 독립적으로 작동.
> 태블릿 가로(PC 레이아웃)여도 터치 기기면 커서 없음.
> PC에서 창을 줄여도 마우스 있으면 커서 유지.

> 각 섹션의 환경별 세부 동작은 해당 섹션 "반응형 레이아웃" 항목에 기재.

---

## 7. UI 공통 언어

### 커스텀 커서 (PC 전용, 전역)

**PointRingCursor** — 전역 적용.

| 상태                  | 표현                     |
| --------------------- | ------------------------ |
| 기본                  | 작은 도트 + 지연 추적 링 |
| 링크/버튼/타이틀 호버 | 링이 사각형으로 변형     |

### 스크롤/스와이프 힌트 애니메이션

| 용도                   | 방식                                            |
| ---------------------- | ----------------------------------------------- |
| 세로 스크롤 힌트       | 얇은 선이 위아래로 늘어났다 줄어드는 애니메이션 |
| 가로 스와이프 힌트     | 얇은 선이 좌우로 늘어났다 줄어드는 애니메이션   |
| 가로 스와이프힌트 보조 | 작은 네모 좌우로 이동 방식                      |

### 푸터에서 커스텀커서

Get in touch에 마우스 오버시 동그라미 큰도형안에 Contact. - PointRingCursor 예외
(첨부 이미지 있음)

---

## 8. 네모 Morphing 전체 시퀀스

네모는 홈페이지 전체를 관통하는 하나의 살아있는 오브젝트.
섹션 전환 시 변형이 시작되고, 다음 섹션에 어느 정도 진입했을 때 완성됨.
스크롤 progress에 연동 — 역방향 되감기도 자연스럽게 동작.

```
히어로
└── 네모 상태: [결] 뒤 작은 네모 (Shared Element 시작점)
    ↓ 스크롤 — 작은 네모가 확장되며 Pain & 공명 전체 배경이 됨
Pain & 공명
└── 히어로 네모: Pain & 공명 배경(`#0D1A1F`)으로 전환 완료 (Shared Element 역할 종료)
    새 네모 등장: `#f0ebe3` 테두리, 왼쪽 위치
    ↓ 테두리 → 크림 채워짐 → 틸로 색 전환 + 세로 긴 박스로 변형
메시지
└── 진입 후 완성: 틸(`#0891b2`) 세로 긴 작은 박스, 중앙
    ↓ 변형 시작
ForWho
└── 진입 후 완성: 버스 사진이 채워진 가로 박스 (위아래 배경 보임)
    ↓ 이후 미정
브랜드 스토리 / CTA
└── 미정
```

참고링크(https://www.easymedia.net/kr/main)

#### 구현 방식

특정 컴포넌트(히어로 섹션 [결] 뒤의 네모)가 섹션 스크롤에 따라 사라지지 않고 'Shared Element'로 유지되면서 변형되는 인터랙션으로 구현.

1. 시작 상태: 히어로 섹션 [결] 뒤 특정 위치에서 특정 크기와 색상으로 시작.
2. 전환 과정 (ScrollTrigger Scrub):
   - 히어로 → Pain & 공명: 작은 네모가 확장되며 섹션 전체 배경(`#0D1A1F`)이 됨. (Shared Element 역할 종료)
   - Pain & 공명: 새 테두리 네모 등장. 안에서 Pain 포인트, 공명 문장 진행.
   - Pain & 공명 → 메시지: 테두리 → 크림 채워짐 → 틸 세로 긴 박스로 변형.
   - 메시지 → ForWho: 세로 긴 박스 → 가로 박스 확장 + 버스 사진 채워짐.
3. 구현 원칙:
   - Pain & 공명 이후 네모는 DOM에서 단일 `div`를 유지할 것.
   - 캔버스나 복잡한 방식 대신, 레이아웃 위에 떠 있는 전역 오버레이 요소 방식 (`position: fixed`).
   - GSAP ScrollTrigger의 `scrub`을 사용하여 스크롤 진행도에 1:1 대응시킬 것.
   - 변형되는 property: `width` / `height` / `background-color` / `border` / `transform(translate)` / `background-image`.
   - 역방향 스크롤 시 자연스럽게 되감기 — scrub이 자동 처리.

---

## 9. 전체 감정 호

```
낯섦(히어로) → Pain & 공명 → 이해(메시지) → 나 얘기네(ForWho) → 신뢰(브랜드 스토리) → 행동(CTA)
```

---

## 10. 헤더 / 네비게이션

### 구조

- `position: fixed`, 투명 배경 — 별도 헤더 바 없음. 섹션 배경 위에 float
- 로고: 왼쪽 상단 / 햄버거: 오른쪽 상단
- 히어로에서는 헤더가 히어로 콘텐츠의 일부 — 빅타이포가 히어로 안에 있다가 스크롤하면서 헤더 자리로 이동. 히어로와 헤더가 분리된 게 아니라 하나로 연결된 구조.

### 로고 시스템

**브랜드 어원**: Rectangle에서 시작. rec**t**angle의 t를 + 로 표현 → REC+ANGLE = 네모결

**로고 컬러**

| 요소      | 상태          | 표현               |
| --------- | ------------- | ------------------ |
| 네모 / ON | 기본 텍스트값 | —                  |
| △         | 틸 filled     | `#0891b2` 채움     |
| ○         | 틸 outlined   | `#0891b2` 테두리만 |

### 컬러

| 상태           | 로고/햄버거 색 |
| -------------- | -------------- |
| 배경 어두울 때 | `#ffffff`      |
| 배경 밝을 때   | `#0d1a1f`      |

> 배경색 인터폴레이션과 연동하여 자동 전환

### 햄버거 버튼

- 네모 테두리 안에 세 줄 (`≡`)
- 배경: 투명 + 테두리만 (색은 로고와 동일하게 연동)
- 닫힘: `≡` / 열림: `×` (선이 rotate되며 변형)
- OFF 상태: 숨김 / ON 전환 완료: fade-in

### 사이드 메뉴

- 메인 콘텐츠가 왼쪽으로 밀리면서 뒤에 있던 메뉴가 드러나는 방식 (참조: outspring.co.kr)
- 메뉴 배경: `#0891b2` (브랜드 틸)
- 텍스트: `#ffffff` (흰색)
- 호버: `#E8734A` (오렌지) — 개발하면서 확정
- 메뉴 항목: 크게, 여백 넉넉하게 (에디토리얼 그리드 언어)

참고 https://www.outspring.co.kr/

### Logo Transform (홈 전용)

히어로부터 ForWho까지 스크롤하면서 로고가 브랜드 어원을 경험하게 하는 여정.
OFF→ON 전환은 딱 한 번에 교체, 나머지는 스크롤 progress에 따라 점진적으로 변형.
Fluid Typography (`clamp()`) 적용 — 뷰포트에 따라 빅타이포 크기 자동 조절.

```
히어로 OFF (정지)
└── 빅타이포: 네모△/○OFF (헤더 자리, 상단)
    햄버거: 숨김

OFF → ON 전환 (트리거 방식 미정)
└── 네모△/○OFF → 네모△/○ON (딱 한번에)
    햄버거: fade-in 등장

히어로 → Pain & 공명 (스크롤 — Logo Transform)
└── △/○ON 빠짐 → 네모만 남음
    네모 서서히 작아지면서 헤더 자리로 이동
    스크롤 threshold 넘으면 position: fixed 전환

Pain & 공명
└── 헤더: 네모 고정

Pain & 공명 → 메시지 (스크롤 — Logo Transform)
└── 네모 → RECTANGLE 텍스트 전환
    스크롤하면서 T → + 로 변형

메시지
└── 헤더: REC+ANGLE 고정

메시지 → ForWho (스크롤 — Logo Transform)
└── REC+ANGLE → 네모 (한글) 전환
    △/○ 등장하면서 네모△/○ON 완성

ForWho ~ 이후
└── 헤더: 네모△/○ON 고정 유지
```

참고사이트 (https://www.easymedia.net/kr/main)

#### 구현 방식

> **"브랜드의 어원을 경험하게 하는 8단계 'Logo Journey' 인터랙션으로 구현. 헤더 로고가 단독 요소로 존재하며 스크롤에 따라 상태가 변하는 방식."**
>
> **1. 기본 원칙**:
>
> - **Single Source**: 헤더 로고와 히어로 빅타이포를 별도로 만들지 말고, 하나의 
>   Header 컴포넌트 내 `div`가 모든 섹션을 관통하며 유지될 것.
> - **Fluid Typography**: `clamp()`를 사용하여 뷰포트에 따라 로고 크기가 자동 조절되게 할 것.
> - **Scroll Sync**: 모든 변형은 GSAP ScrollTrigger의 `scrub`을 통해 스크롤 진행도에 1:1로 대응할 것.
>
> **2. 로고 구성 규칙**:
>
> - 우리 브랜드명은 **한글 '[네모]' + 도형 '[△/○]' + 영문 '[ON]'**의 조합이다.
> - '[△/○]'는 콜론(:) 역할을 하는 **세모와 동그라미 도형**이다.
> - 영문 'NEMO'는 절대로 사용하지 않으며, 오직 한글 '네모'만 사용한다.
>
> **3. 8단계 여정 설계**:
>
> - (1) **Hero OFF**: 헤더 위치에서 빅타이포 크기의 **[네모] + [△/○] + [OFF]**로 시작.
> - (2) **OFF → ON**: 스크램블 효과와 함께 **[네모] + [△/○] + [ON]**으로 전환. 햄버거 등장.(이때만 트리거 방식)
> - (3) **Hero → Pain**: 도형 **[△/○]**과 **[ON]**은 사라지고, **[네모]**만 소리 없이 작아지며 헤더 로고 크기로 안착.
> - (4) **Pain 구간**: 헤더 위치에 '네모' 고정.
> - (5) **Pain → Message**: '네모'에서 [RECTANGLE]로 텍스트 전환. 스크롤하며 'T' 문자가 '+' 기호로 모핑(Morphing).
> - (6) **Message 구간**: 'REC+ANGLE' 고정.
> - (7) **Message → ForWho**: **[REC+ANGLE]**이 다시 한글 **[네모]**로 돌아오고, 사라졌던 **[△/○]**와 **[ON]**이 다시 합쳐지며 완성.
> - (8) **마지막**: 최종 완성된 **[네모] + [△/○] + [ON]** 형태 유지.
>
> **4. 기술적 제약**:
>
> - 브랜드명은 무조건 한글 **'네모'**만 사용 (영문 NEMO 금지).
> - 전환 시 레이아웃이 튀지 않도록 `fixed` 위치와 `x, y` 트랜스폼을 조합할 것.

### 다른 페이지

- 헤더: `네모△/○ON` 고정
- Logo Transform 없음

---

## 11. 푸터

모든 페이지 공통. 전역 고정.

#### 배경색

`#0a0a0a` (다크) — CTA 터미널과 자연스럽게 이어지는 구조

#### 레이아웃 방식

```
[우측 상단]
Get in touch
(호버시 PointRingCursor → 흰 원형 "contact" 커서로 교체) : 예시 이미지 있음
(클릭시 /contact 이동)

[중앙]
네모:ON 빅타이포 (풀블리드)
△○ 틸색(`#0891b2`), 나머지 기본 텍스트

[하단 한 줄]
Threads  Instagram  |  © 2026 네모결스튜디오. All rights reserved.
```

#### 반응형

모바일: 하단 두 줄로

```
Threads  Instagram
© 2026 네모결스튜디오. All rights reserved.
```

#### 진입 방식

푸터는 뒤에 고정(`position: fixed` 하단). 메인 콘텐츠 전체가 스크롤하면 위로 올라가면서 뒤에 있던 푸터가 자연스럽게 드러나는 방식. CSS만으로 구현 가능.

#### 사이드 메뉴 연동

사이드 메뉴 열릴 때 — 헤더 + 콘텐츠 + 푸터 전체 래퍼가 함께 왼쪽으로 밀림. 푸터만 따로 움직이지 않음.

---

## 12. 각 페이지/섹션 기획 템플릿

각 섹션/페이지 기획 시 아래 항목을 순서대로 채운다.
전역에서 정해진 항목(컬러 토큰, 반응형 기준, 기술 스택)은 참조만 하고 섹션별 세부 내용만 기재.

```
페이지/섹션명
├── 배경색 / 색상 참조     — 어떤 컬러 토큰 쓰는지
├── 레이아웃 방식          — 전체 구조
├── 반응형 레이아웃        — PC/모바일별 레이아웃 및 동작 차이
├── 스크롤 정책            — 진입/진행/이탈 조건
├── 진행 시퀀스            — 인터랙션 흐름 단계별
├── 콘텐츠 내용            — 카피, 데이터 (data/ 파일 참조)
├── 네모 모핑              — 진입 상태 / 퇴장 방식
└── 사용 기술              — 이 섹션에서 실제 쓰이는 기술
```

---

## 13. 홈 섹션 상세 기획

### 전체 섹션 구조

```
0  헤더 (모든페이지 적용)
1  Hero : 다크 → 크림/오프화이트
2  Pain & 공명 : 다크
3  Message (What we do) : 크림
4  ForWho :크림
5  Brand Story : 틸 → 2분할(틸 | 크림/오프화이트)
6  브랜드 진단 (CTA) : 다크
7  푸터 (모든 페이지 적용)
```

### 섹션 01. Hero

**낯섦 : 꺼짐 → 켜짐**

#### 배경색 / 색상 참조

히어로 전용 OFF/ON 동적 컬러 시스템 사용 (전역 토큰과 별도).

| 용도            | OFF 모드 (Dark)          | ON 모드 (Light)       | 설명                   |
| --------------- | ------------------------ | --------------------- | ---------------------- |
| 배경            | `#0a0a0a`                | `#faf7f2`             | 전체 배경              |
| 기본 텍스트     | `#f0ebe3`                | `#0d1a1f`             | 슬로건 및 타이틀       |
| 강조            | `#e8d5b0`                | `#0891b2`             | 포인트 요소            |
| 보조 강조       | `#c4a882`                | `#0e7490`             | 보조 장식 및 셰이프 선 |
| 노이즈/파티클   | `rgba(240,235,227,0.1)`  | `rgba(13,26,31,0.1)`  | 배경 질감              |
| 텍스트 특수효과 | `rgba(240,235,227,0.45)` | `rgba(13,26,31,0.25)` | 스크램블 효과 투명도   |

#### 레이아웃 방식

풀블리드 배경. 빅타이포(로고)가 상단에 위치하며 헤더와 통합된 구조.
Fluid Typography (`clamp()`) 적용.

**OFF 모드**

```
[상단] 네모△/○OFF 빅타이포 (헤더 통합)
       햄버거: 숨김
[배경] 다크(#0a0a0a)
       중앙 상단 베이지(#f0ebe3) 빛 — radial-gradient (램프 이펙트)
       파티클: 네모/세모/동그라미 자유 부유 (마우스/터치 밀어내기)
[중앙] "브랜드를 켜다" (스크롤 또는 일정 시간 경과 시 자동 활성화)
[하단] 슬로건 카피
       흐릿한 [아이디어를|생각을|확신을|방향을], 작동하는 브랜드로 — RotatingText (GSAP Refactored)
```

**ON 모드**

```
[상단] 네모△/○ON 빅타이포 (헤더 통합)
       햄버거: fade-in 등장 (ON 모드 안착 시)
[배경] 크림(#faf7f2)
[중앙] 도형 시퀀스 (ShapesStage)
       카피 3줄 (개발시 레이아웃 확정)
[하단] "불안을 끄고, 기준을 켭니다" — TrueFocus
       스크롤 힌트 (얇은 선 위아래 확장)
```

> ON 모드 세부 레이아웃은 개발시 실제로 보면서 확정

#### 반응형 레이아웃

- 모바일: "브랜드를 켜다" 항상 보임 (PC는 스위치 호버시)
- 빅타이포 크기: Fluid Typography `clamp()` 로 뷰포트에 따라 자동 조절
- 세부 레이아웃 재조정 필요 (PENDING — 개발시 확정)

#### 스크롤 정책

**OFF 상태**

- 다음 섹션으로 스크롤 불가
- 진입 후 4초 이전 스크롤 감지 → ON 전환 안됨, 스위치 힌트 애니메이션 강하게 강조
- 진입 후 4초 이후 스크롤 감지 → ON 전환 트리거 (스크롤 양 무관, 한 번만 처리)
- 7초 아무 인터랙션 없을 시 자동 ON 전환

**ON 전환 후**

- 요소 순차 등장 완료 후 다음 섹션 스크롤 가능
- 개발 후 답답하면 조정

**타이머 관리**

- 수치값: `constants/hero.ts` — `HERO_TIMERS` 객체로 한곳에서 관리
- 로직: `hooks/useHeroTimer.ts` — scrollLock, autoOn, reset 등 타이머 로직 분리

```ts
// constants/hero.ts
export const HERO_TIMERS = {
  scrollLockDuration: 4000, // 스크롤 트리거 잠금 (4초)
  autoOnDelay: 7000, // 자동 ON 전환 (7초)
  // 시퀀스 타이밍값들도 여기서 관리 (개발하면서 추가)
};
```

> 스크롤 잠금 시간(4초), 자동 전환 시간(7초) — 개발 후 실제로 보면서 조정

#### 진행 시퀀스

**OFF 상태**

- 파티클: 네모/세모/동그라미 자유 부유 (마우스/터치 밀어내기 인터랙션)
- "브랜드를 켜다": 은은한 Glow 및 블러 효과
- 카피: 흐릿한 [아이디어를|생각을|확신을|방향을], 작동하는 브랜드로 — RotatingText

**OFF → ON 전환**

| 단계 | 효과                                                                                                          |
| ---- | ------------------------------------------------------------------------------------------------------------- |
| 1    | 파티클이 특정 지점으로 모여드는 움직임으로 전환                                                               |
| 2    | 베이지 빛(radial-gradient)이 확장되면서 틸(`#0891b2`)로 색 전환                                               |
| 3    | 틸 네모가 와이프되며 배경을 덮음                                                                              |
| 4    | 배경 크림(`#faf7f2`)으로 전환, ON 모드 완성                                                                   |
| 5    | 빅타이포 네모△/○OFF → 네모△/○ON — 스크램블 효과 (글자가 무작위 특수문자로 섞이다가 서서히 목적 단어로 맞춰짐) |
| 6    | 햄버거 + 전구 아이콘 fade-in                                                                                  |

**ON 상태 카피 인터랙션**

- "감성" 클릭/터치 → ShapesStage에서 ○만 표시
- "구조" 클릭/터치 → ShapesStage에서 △만 표시
- "결" 클릭/터치 → ShapesStage에서 □만 표시
- 기본 상태 → 세 도형 모두 표시

#### 콘텐츠 내용

| 위치            | OFF                                | ON                                                                                                               |
| --------------- | ---------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| 빅타이포 (헤더) | 네모△/○OFF                         | 네모△/○ON                                                                                                        |
| 중앙            | 브랜드를 켜다 (glow 효과) + 스위치 | 도형 시퀀스 + 카피                                                                                               |
| 카피 컬러       | —                                  | "감성" 틸 계열 A / "구조" 틸 계열 B (서로 약간 다름, 개발시 확정) / "결" — `#0D1A1F` 배경 네모 박스 안 흰 텍스트 |
| 하단            | 흐릿한 RotatingText 슬로건         | "불안을 끄고, 기준을 켭니다" TrueFocus                                                                           |

**ON 모드 카피 3줄 (ShapesStage 옆)**

```
감성 위에 구조를 더해
당신의 결로
브랜드를 켭니다
```

- "감성": ○ 연동 / "구조": △ 연동 / "결": □ 연동
- 클릭/터치 시 해당 도형만 ShapesStage에 표시
- 컬러: "감성" 틸 계열 A / "구조" 틸 계열 B / "결" — `#0D1A1F` 배경 네모 박스 안 흰 텍스트

#### 네모 모핑

- 시작점: [결] 뒤 작은 네모 박스 (Shared Element)
- 퇴장: 스크롤 시 확장되며 Pain & 공명 섹션 전체 배경(`#0D1A1F`)이 됨
- Logo Transform은 네모 모핑과 별개로 동시 진행 (△/○ON 빠지고 네모만 남으며 헤더로 이동)

#### 사용 기술

- Canvas API + `useParticles` (파티클)
- CSS `radial-gradient` (램프 이펙트 — 베이지 빛)
- GSAP (와이프 트랜지션, 빛 확장, Logo Transform)
- RotatingText (React Bits) - https://reactbits.dev/text-animations/rotating-text 학습필요
- TrueFocus (React Bits) - https://reactbits.dev/text-animations/true-focus 학습필요
- Fluid Typography `clamp()` (빅타이포 반응형)
- CSS 변수 동적 주입 (OFF/ON 컬러 전환)

#### 컴포넌트 재설계 필요

가로스크롤 관련 코드 제거 후 전면 재설계.
기존 `HeroBigTypo` 컴포넌트 역할 변경 (하단 → 상단, 헤더 통합).

---

### 섹션 02. Pain & 공명

#### 배경색 / 색상 참조

`#0D1A1F` (섹션 다크 배경 토큰)
네모: `#f0ebe3` (크림 텍스트/요소 토큰)

#### 레이아웃 방식

풀블리드 다크 배경. 네모 약간 왼쪽 위치. 페인포인트 및 공명문장은 네모 안에 나옴

#### 반응형 레이아웃

네모 크기/위치 수치 조정 외 레이아웃 구조 변경 없음. 개발시 확정.

#### 스크롤 정책

- PC / 모바일 모두 세로 스크롤
- GSAP ScrollTrigger pin — 스크롤 progress에 모든 인터랙션 연동
- **끝까지 봐야 다음 섹션(메시지)으로 이동**
- 위로 스크롤 시 역순 되감기 → Pain 첫 번째까지 돌아와야 히어로로 이동
- 네모도 스크롤 progress에 따라 역방향으로 자연스럽게 움직임

#### 진행 시퀀스

**1단계 — Pain 포인트 5개**
스크롤마다 Pain 포인트 교체. 뷰포트 상단에 감정키워드 누적 (뒤로 스크롤 시 pop).

**2단계 — 네모 안에서 문장 등장**

스크롤에 의해 페인포인트 5개가 다 보이고 나면 스크롤마다 한 단락씩 등장.

```
나는,
당신은,
우리는 지금 설명 가능한가요?
```

**3단계 — FallingText 트리거**
스크롤 특정 지점 도달 시 (나는 당신은 우리는 지금 설명가능한가요?까지 다 나온 시점)
스크롤을 한번 더 하면 상단 누적 키워드들이 우수수 떨어짐

**4단계 — 네모 전환**
왼쪽 → 중앙 이동 + 테두리 → `#f0ebe3` 채워짐 (공명 문장이 나오기 전)

**5단계 — 공명 문장**

```
의미를 부여하면 의미가 됩니다.
의미가 있으면 선명해집니다.
선명해지면 설명이 가능해지고,
설명이 가능해지면 연결됩니다.
```

크림 박스 안, 텍스트 `#0D1A1F`

#### 콘텐츠 내용

데이터파일 만들어서 참고

| #   | Pain 포인트                                                   | 감정 키워드                  |
| --- | ------------------------------------------------------------- | ---------------------------- |
| 1   | 지금 돈 쓰는 게 맞나, 괜히 날릴까 불안해요                    | 불안 / 돈낭비 / 망설임       |
| 2   | 처음부터 제대로 하고 싶은데, 뭘 어떻게 시작해야 할지 막막해요 | 막막 / 막연함 / 방향상실     |
| 3   | 일단 홈페이지만 있으면 매출이 생기겠죠?                       | 착각 / 기대 / 허탈           |
| 4   | 가격은 올리고 싶은데, 그만한 이유를 설명하기 어려워요         | 설명불가 / 설득안됨 / 답답함 |
| 5   | 조금만 더 알아보자 하다가, 시간만 계속 지나가요               | 미룸 / 무기력 / 제자리       |

#### 네모 모핑

- 진입 상태: 히어로 네모가 배경이 된 후 새 네모 등장 — `#f0ebe3` 테두리, 왼쪽 위치 (수치 미정)
- 퇴장: 테두리 → 크림(`#f0ebe3`) 채워짐 → 틸(`#0891b2`)로 색 전환 + 세로 긴 작은 박스로 변형 동시 진행
- 배경색 인터폴레이션: `#0D1A1F` → `#f7f1e9` 서서히 전환

#### 사용 기술

- GSAP ScrollTrigger pin
- Matter.js FallingText - [React Bits - Falling Text](https://reactbits.dev/text-animations/falling-text?trigger=scroll) 학습필요
- GSAP lerp (배경색 인터폴레이션)

**참고**: https://reflexion.co.kr/m/about.php (모바일 히어로 — Pain 포인트 넘어가는 방식)

---

### 섹션 03. Message

#### 배경색 / 색상 참조

`#f7f1e9` (라이트 배경 토큰, 개발 시 `#faf7f2`와 눈으로 최종 결정)
네모: `#0891b2` (브랜드 틸 토큰)

#### 레이아웃 방식

라이트 배경. 네모 세로 긴 박스 중앙 고정. 텍스트가 네모를 통과하며 올라오는 구조.
사이드 세로 고정 (메시지 텍스트 그룹과 완전히 별개 영역)

- 뒤 레이어: FUNNEL (영어, 더 흐릿) — 배경 장식
- 앞 레이어: 브랜딩 → 디자인시스템 → 로고·웹/앱 → 자동화 (한국어, 흐릿하지만 FUNNEL보단 선명)
- 네모가 자리잡을 때 같이 등장, 이후 고정 유지

**참고**: https://www.easymedia.net/kr/main (네모 배경 고정 + 텍스트 reveal 방식)
[Text Reveal | React Components & Templates](https://magicui.design/docs/components/text-reveal) 참고 후 필요시 학습필요

#### 반응형 레이아웃

사이드 FUNNEL 레이어(영문/한글) — 모바일에서는 사이드 고정 대신 네모 박스 안 배경 텍스트로 처리.
FUNNEL(영문) + 브랜딩→디자인시스템→로고·웹/앱→자동화(한국어) 세로 나열, 흐릿하게 표시.
메시지 카피는 그 위 레이어로 올라옴. 네모 세로 박스 중앙 고정 구조는 유지.

#### 스크롤 정책

GSAP ScrollTrigger scrub. 스크롤 progress 연동 — 그룹 전체가 함께 올라오고 reveal되는 속도를 유저가 컨트롤.

#### 진행 시퀀스

네모가 중앙으로 자리잡으며 시작. 3개 그룹이 순서대로 올라오고 reveal되고 사라지는 구조.

**텍스트 올라오는 방식**

- 스크롤 progress 연동으로 그룹 전체가 함께 올라옴

**reveal 방식**

- 그룹 전체 올라온 후 stagger reveal (흐릿 → 선명)
- 첫 문장 reveal 진행 중 → 다음 문장 reveal 시작 (완전 동시 아님)
- reveal 완료 후 사라지고 다음 그룹 시작

**텍스트 컬러 시스템**

| 상태             | 배경 위   | 네모 위                 | 네모 안 FUNNEL 텍스트 위(모바일) |
| ---------------- | --------- | ----------------------- | -------------------------------- |
| Reveal 전 (흐릿) | `#C4BDB4` | `rgba(240,235,227,0.3)` | 개발시 확정                      |
| Reveal 후 (선명) | `#0D1A1F` | `#f0ebe3`               | 개발시 확정                      |

#### 콘텐츠 내용

**① 그룹** (올라온 후 stagger reveal → 사라짐)

```
브랜딩부터 웹까지, 하나의 기준으로 설명 가능하게.
설명이 가능해지면 연결됩니다.
```

**② 그룹** (scroll progress 연동 → 올라온 후 stagger reveal → 사라짐)

```
단지 있어야 하니까 만드는 브랜드엔 관심이 없습니다.
단순히 예쁜 웹사이트를 만들지 않습니다.
```

**③ 그룹** (scroll progress 연동 → 올라온 후 stagger reveal)

```
당신의 언어를 고객의 언어로.
당신의 가치를 고객 선택 이유로.
당신의 감각을 고객이 이해하는 구조로.

설명이 가능해지면 연결됩니다.
```

#### 네모 모핑

- 진입 상태: 틸(`#0891b2`) 세로 긴 작은 박스, 중앙 고정
- 퇴장: 세로 긴 틸 박스 → 가로 가득 채우는 박스로 커짐 (위아래 배경 보임) + 버스 사진 채워짐

#### 사용 기술

- GSAP ScrollTrigger
- clip-path 두 레이어 (배경용 / 네모용)

---

### 섹션 04. For Who

#### 배경색 / 색상 참조

`#f7f1e9` (라이트 배경 토큰)

#### 레이아웃 방식

라이트 배경. 진입 시 버스 사진 풀블리드 박스 → 캐러셀로 전환.
상단 문장이 사진 위 → 박스 위로 이동.

#### 반응형 레이아웃

인디케이터 없음, 스와이프 힌트 모션으로 대체 (얇은 선 좌우 확장 애니메이션) — PC/모바일 동일.
PC는 다음 타겟 양옆에 조금 보임, 모바일은 한 화면에 해당 카드만 하나만 보임
스와이프 입력 방식이 달라 캐러셀 컴포넌트를 PC/모바일 각각 별도 구현.

- PC: 드래그(클릭스와이프) 이벤트 기반 - 모바일: 터치 스와이프 이벤트 기반
  컴포넌트 PC/모바일 분리 구현.
  카드 안 텍스트와 사진 인물 위치 관계 — 개발시 확인 필요.

#### 스크롤 정책

- 캐러셀 안에서는 스와이프로 타겟 넘김
- 스크롤하면 다음 섹션으로 이동

#### 진행 시퀀스

**진입 시퀀스**

1. 버스 사진 박스 완성 (메시지 섹션에서 모핑되어 진입)
2. 상단 문장이 사진 위에 등장
3. 박스가 약간 줄어들면서 캐러셀 형태로 전환
4. 상단 문장은 박스 위로 이동
5. 박스 사진은 첫 번째 타겟 사진으로 전환
6. 첫 번째 콘텐츠 내용

**캐러셀**

| 항목           | 내용                                              |
| -------------- | ------------------------------------------------- |
| 한 화면에 카드 | 1개                                               |
| 전환 방식      | 스와이프 (PC: 드래그/클릭, 모바일: 터치 스와이프) |
| 카드 전환 효과 | 모핑                                              |
| 인디케이터     | 없음                                              |
| 스와이프 힌트  | 얇은 선 좌우 확장 애니메이션                      |
| 컴포넌트       | PC / 모바일 분리                                  |

#### 콘텐츠 내용

`data/home/forwho.ts` 만들어서 참고

\```ts
type ForWhoItem = {
id: number
target: string // 타겟명 (예비창업가)
flow: string // 전환 (아이디어 → 브랜드)
description: string // 설명
philosophy: string // 관계 철학
image: {
src: string // 이미지 경로
alt: string
objectPosition: string // 인물 위치 기준 (예: 'left top')
}
}
\```

**섹션 상단 문장**

```
이런 전환의 순간에 함께합니다.
```

**타겟 5개**

| #   | 타겟                   | 전환              | 설명                                                                    |
| --- | ---------------------- | ----------------- | ----------------------------------------------------------------------- |
| 1   | 예비창업가             | 아이디어 → 브랜드 | 아이디어는 있지만, 어디서부터 어떻게 해야할지 막막한 분                 |
| 2   | 성장모색 대표님        | 오프라인 → 온라인 | 기존 브랜드를 디지털 환경에 맞게 전환하고 싶은 대표님                   |
| 3   | 확장을 준비하는 대표님 | 브랜드 → 시스템   | 리뉴얼과 자동화를 통해 더 잘 작동하는 브랜드를 만들고 싶은 대표님       |
| 4   | 개인브랜드 & 전문가    | 전문성 → 브랜드   | 자신만의 철학과 전문성을 온라인에서 차별화된 존재감으로 만들고 싶은 분  |
| 5   | 스타트업               | MVP → 브랜드      | 투자자와 고객 모두에게 설명가능하고 확장 가능한 브랜드 구조가 필요한 팀 |

**카드 안 텍스트 구조 (순서 확정)**

```
타겟명
흐름 (아이디어 → 브랜드 형식)
설명
——— (얇은 선 구분)
관계 철학 (네모:ON 역할)
```

- 텍스트 위치: 카드별로 사진 인물과 안 겹치는 곳 — 개발할 때 실제로 보면서 확정
- 폰트 크기 등 세부 스타일: 개발할 때 확정

**타겟별 관계 철학**

| #   | 타겟                   | 관계 철학                                                       |
| --- | ---------------------- | --------------------------------------------------------------- |
| 1   | 예비창업가             | 첫 브랜딩의 막막함을 함께 정리해가는 멘토                       |
| 2   | 성장모색 대표님        | 기존 가치를 디지털 안에서 다시 작동하게 만드는 파트너           |
| 3   | 확장을 준비하는 대표님 | 더 크게 움직이기 위해 브랜드의 작동 방식을 함께 설계하는 파트너 |
| 4   | 개인브랜드 & 전문가    | 개인의 철학과 전문성을 세상에 알리는 조력자                     |
| 5   | 스타트업               | 빠르게 검증하고 단단하게 쌓아가는 빌더                          |

**카드 이미지 스펙**

| 항목        | 내용                                                   |
| ----------- | ------------------------------------------------------ |
| 원본 해상도 | 1920x1080                                              |
| CSS         | `object-fit: cover` + `object-position` 인물 위치 기준 |
| 텍스트 위치 | 사진 내 인물 없는 여백 쪽                              |

**타겟별 사진 스펙**

전체 무드: surreal, dreamlike, soft natural light, cinematic still, wide shot

| #   | 타겟                | 인물                   | 위치        | 배경                           | object-position | 파일명(/public/images/home/forwho) |
| --- | ------------------- | ---------------------- | ----------- | ------------------------------ | --------------- | ---------------------------------- |
| 0   | 진입 (버스)         | VW 버스 뒷모습         | 중앙 도로   | 붉은 암벽 사막, 광활한 도로    | `center`        | bus-journey.jgp                    |
| 1   | 예비창업가          | 남성, 아래를 보는      | 왼쪽 상단   | 크림/핑크 노을, 넓은 평원      | `left top`      | target1.png                        |
| 2   | 성장모색 대표님     | 남성, 문을 나가는      | 왼쪽 하단   | 초록 들판 + 핑크 노을, 문 하나 | `left bottom`   | target2.png                        |
| 3   | 확장 준비 대표님    | 여성, 위를 바라보는    | 오른쪽 하단 | 틸 하늘, 높은 곳               | `right bottom`  | target3.png                        |
| 4   | 개인브랜드 & 전문가 | 여성, 그림 그리는      | 중앙        | 파스텔 바다                    | `center`        | target4.png                        |
| 5   | 스타트업            | 실루엣 여러 명, 다이빙 | 왼쪽        | 틸+오렌지 노을, 바다           | `left center`   | target5.png                        |

#### 네모 모핑

- 진입 상태: 버스 사진이 채워진 가로 박스 (위아래 배경 보임)
- 퇴장: 스크롤에 맞춰 캐러셀 형태 위로 올라가며 퇴장. 배경색 크림 → 틸로 인터폴레이션 전환

#### 사용 기술

- GSAP ScrollTrigger
- 드래그/터치 스와이프 핸들러 (PC/모바일 분리)

---

### 섹션 05. BrandStory

**역할**: 신뢰 구간. "누가 만드는가"를 통해 신뢰 형성.

#### 배경색 / 색상 참조

- 진입: 틸 단색 (`#0891b2`) — ForWho 크림에서 배경색 인터폴레이션(GSAP lerp)으로 전환
- 중간: 좌우 분할 (틸 | 크림/흰색) → 좌우 반전 (크림/흰색 | 틸)
- 퇴장: 단색으로 통일 — CTA 배경색에 맞춰 확정

#### 레이아웃 방식

풀블리드. 스크롤 progress 연동으로 배경과 텍스트가 함께 움직이는 구조.
네모 오브젝트 별도 진입 없음 — 텍스트가 틸 박스 안에 담겨 움직이는 것이 네모 언어.

#### 반응형 레이아웃

PC 좌우 2분할 → 모바일 상하 2분할.
한 화면에 틸+크림 반반 구성 유지.

| 구간 | PC                        | 모바일                    |
| ---- | ------------------------- | ------------------------- |
| ②    | 좌: 크림(텍스트) / 우: 틸 | 상: 크림(텍스트) / 하: 틸 |
| ③    | 좌: 틸 / 우: 크림(텍스트) | 상: 틸 / 하: 크림(텍스트) |

텍스트 움직임 방향도 전환됨.

| 구간     | PC            | 모바일              |
| -------- | ------------- | ------------------- |
| ② 텍스트 | 위에서 아래로 | 오른쪽에서 왼쪽으로 |
| ③ 텍스트 | 아래에서 위로 | 왼쪽에서 오른쪽으로 |

#### 스크롤 정책

전체 스크롤 progress 연동 (scrub). 유저가 속도 컨트롤.

#### 진행 시퀀스

```
① 틸 단색 배경
   가운데 텍스트:
   "저는 늘 경계에 있었습니다.

   호주 Macquarie 대학에서 IT를 공부했습니다.
   한국인도 호주인도 아닌 경계에서
   제 정체성을 계속 설명해야 했습니다.

   그래서 압니다.
   설명되지 않으면 연결되지 않는다는 것을."

↓ 스크롤

② 배경 좌우 분할 (좌: 크림/흰색 | 우: 틸)
   왼쪽 흰색 영역 — 텍스트(틸 박스)가 위에서 중앙까지 내려옴:
   "IT, 방송, 요리, 목수, PM, 디자인 등
   서로 다른 일을 오래 건너오며
   하나를 배웠습니다.
   사람이 이해하고 선택하는 방식은 닮아 있다는 것을.

   그리고 이 모든 경험이
   당신의 사업을 이해하는 힘이 되었다는 것을 이제 압니다.

   그래서 당신의 언어와 고객의 언어 사이,
   그 경계에서 브랜드를 번역합니다."

↓ 스크롤

③ 배경 좌우 반전 (좌: 틸 | 우: 크림/흰색)
   오른쪽 흰색 영역 — 텍스트(틸 박스)가 아래에서 중앙까지 올라옴:
   "감성 위에 구조를 더하고,
   구조 안에 감성을 불어넣는 일.

   보이지 않는 분위기를 언어로 정리하고,
   흐릿한 가치를 작동하는 구조로 바꾸는 일.

   당신의 고유함을 기록하고
   고객과 연결하는 이야기로 만드는 일."

↓ 스크롤

④ 배경 단색으로 통일 (CTA 배경색에 맞춰 확정)
   가운데 텍스트:
   "불안을 끄고, 기준을 켭니다.

   이제 브랜드를 켤 차례입니다."
```

> 틸 박스 형태 여부(②③ 텍스트) — 개발하면서 확정

#### 더 알아보기

브랜드 스토리 섹션 내 `/about` 링크 포함. 단테 소개 및 네모:ON 철학 더 깊이 보기.

#### 네모 모핑

없음. 텍스트가 틸 박스 안에 담겨 움직이는 것이 네모 언어로 작동.

#### 사용 기술

- GSAP ScrollTrigger scrub (스크롤 progress 연동)
- GSAP lerp (배경색 인터폴레이션)
- CSS 좌우 분할 배경 전환

---

### 섹션 06. BrnadDiagnosis - 브랜드 진단(CTA)

**역할**: 행동 유도. 브랜드 진단 페이지로 유입.

#### 배경색 / 색상 참조

- 배경: `#0a0a0a` (다크, 터미널 느낌)
- 브랜드 스토리 마지막 단색 배경에서 CTA 검정 배경이 아래에서 위로 올라오며 덮는 방식 (패널 오버레이 — GSAP ScrollTrigger + pin)

#### 레이아웃 방식

풀블리드 다크 배경. 터미널 화면 콘셉트. 중앙 정렬.

#### 진행 시퀀스

**진입 (스크롤 시 자동 시작)**

```
> 브랜드 켜는 중...
> 브랜드 켜는 중......  [커서 깜빡임]
> 브랜드 진단을 시작하시겠습니까?

[ YES ]  [ NO ]
```

**YES 선택 시**

```
> initializing brand_diagnosis...
> loading user_profile...
> connecting to NEMO:ON...
> ████████████████ 100%
> COMPLETE ✓
→ 브랜드 진단 페이지 자동 이동
```

**NO 선택 시**

```
> ████████████████ (중간에 멈춤)
> ERROR 404: brand_clarity not found
> 브랜드 진단이 필요합니다.
> redirecting...
→ 브랜드 진단 페이지 자동 이동
```

#### 컬러 시스템

| 요소                                                | 색상                         |
| --------------------------------------------------- | ---------------------------- |
| 브랜드 켜는 중... / 브랜드 진단을 시작하시겠습니까? | 터미널 초록 (`#00FF41` 계열) |
| `>` 프롬프트 기호 / COMPLETE ✓                      | 틸 (`#0891b2`)               |
| YES/NO 시퀀스 기본 텍스트                           | 흰색 (`#ffffff`)             |
| ERROR 404 / 브랜드 진단이 필요합니다.               | 레드 (`#E05353`)             |
| redirecting...                                      | 흰색 (`#ffffff`)             |

#### 반응형 레이아웃

컨셉에 맞추어 해당 디바이스 기본방침 적용

#### 네모 모핑

없음.

#### 사용 기술

- GSAP ScrollTrigger + pin (패널 오버레이 — 브랜드 스토리 위로 덮는 방식)
- 타이핑 애니메이션 (터미널 텍스트 순차 출력)
- 프로그레스 바 애니메이션
- DM Mono (터미널 폰트)

---

## 14. 페이지 기획(홈화면 외)

### /About

**목적**: "무엇을 파는가"보다 "우리가 어떤 브랜드인가"를 보여주는 페이지.
서비스 설명은 최소화. 철학 / 태도 / 창업자 관점 / 브랜드 세계관 중심.

**배경**: 크림 (`#f7f1e9`)

#### 섹션 구조

```
Hero → Intro → Brand Philosophy → Name Meaning → Promise → Founder Story → Closing
```

---

#### Hero

```
불안을 끄고, 기준을 켭니다.
네모:ON은 의미를 켜서, 구조가 작동하게 만드는 브랜드입니다.
```

---

#### Intro

```
감성만 있는 브랜드는 예쁘지만 오래가지 않습니다.
구조만 있는 브랜드는 작동하지만 오래 마음에 남지 않습니다.

네모:ON은 감성과 구조의 균형 위에서
브랜드를 선명하게,
이해되게,
작동하게 만듭니다.

우리는 단순히 보이는 결과물을 만드는 팀이 아니라,
왜 이 브랜드가 존재해야 하는지,
고객이 왜 이 브랜드를 선택해야 하는지,
그 이유를 설명할 수 있을 때
비로소 연결이 시작된다고 믿습니다.
```

---

#### Brand Philosophy

```
**설명 가능한 브랜드**

설명되지 않는 브랜드는 연결되지 않습니다.

브랜드의 철학이 언어로 정리되지 않으면
메시지는 흐려지고,
시각은 흔들리고,
고객은 기억하지 못합니다.

브랜드가 스스로를 설명할 수 있을 때
더 오래 이해되고 연결된다고 믿습니다.

의미를 부여하면 의미가 됩니다.
의미가 있으면 선명해집니다.
선명해지면 설명이 가능해지고,
설명이 가능해지면 연결됩니다.

당신의 브랜드, 지금 설명 가능한가요?
```

---

#### Name Meaning

```
**브랜드명의 탄생**

네모:ON은 사각형을 뜻하는 Rectangle에서 시작된 이름입니다.

사각형은 가장 단순하면서도 명확하고 안정적인 형태입니다.
무언가를 담고, 정리하고, 구조화하는 기본 프레임이기도 합니다.
네모:ON은 브랜드 역시 이런 구조 위에서 시작되어야 한다고 믿습니다.

이 안에서 Rectangle은 Rec + Angle로 다시 읽힙니다.

- Rec   기록
- Angle 관점
- +     연결과 확장

대상의 본질을 기록하고,
고유한 관점으로 해석하여,
명확한 브랜드 구조로 확장합니다.

ON, 스위치를 켭니다.
현실에서 작동하는 브랜드, 네모:ON.
```

> Name Meaning 섹션은 텍스트 나열보다 로고 어원을 인터랙션/비주얼로 보여주는 방식 권장 — 개발시 확정

---

#### Promise

```
**우리가 켜는 변화**

네모:ON이 만드는 변화는 거창하지 않습니다.
하지만 분명합니다.

흐릿함은 선명함으로,
감각은 설명 가능함으로,
아이디어는 작동하는 구조로,
"해볼까"라는 망설임은 실행의 상태로 바뀝니다.
```

---

#### Founder Story

```
**왜 이 일을 하나요**

'의미를 부여하면 의미가 된다'는 말을 좋아합니다.

단지 있어야 하니까 만드는 브랜드엔 관심이 없습니다.
왜 이게 있어야만 하는지,
그걸 어떻게 구조화할 수 있을지를 고민하는 일에 끌립니다.

호주 Macquarie 대학에서 IT를 공부했습니다.
한국인도 호주인도 아닌 사이에서
여러 다른 일의 경계를 건너며 살아왔습니다.

IT, 방송, 요리, 목수, PM, 디자인 등
서로 다른 일들 속에서
기술의 언어를 배우고,
사람의 감정을 이해하고,
무언가를 만드는 손의 감각을 익혔습니다.

'왜 있어야만 하는지'를 계속 설명해야 했던 경계에서
뼈저리게 느꼈습니다.
설명되지 않으면 연결되지 않는다는 것을.

그래서 저는 브랜드를 단지 꾸미는 일이 아니라,
당신의 언어와 고객의 언어 사이 그 경계에서,
연결 가능한 형태로 번역하는 일이라고 믿습니다.

설명되지 않으면 연결되지 않습니다.
저는 설명 가능한 브랜드를 만듭니다.
```

---

#### Closing

```
보여지는 브랜드만으로는 충분하지 않습니다.
작동하는 브랜드가 되어야 합니다.

의미를 켜서, 구조가 작동하게.
네모:ON입니다.
```

**CTA**: [스튜디오 보기 →] [진단 시작하기 →]

---

### /Offerings

**목적**: 네모:ON이 무엇을 하는가. 두 가지 실행 방식(Studio / Lab.)의 허브.
**배경**: 크림 (`#f7f1e9`)

#### 섹션 구조

```
Hero → Intro → What we do → Offering 01 Studio → Offering 02 Lab → Closing
```

---

#### Hero

```
**브랜드를 설계하고, 작동하게 만들고, 실험합니다.**

네모:ON은 브랜드를 하나의 결과물로 끝내지 않습니다.
철학을 정리하고, 시각을 만들고, 디지털 구조로 연결하며,
필요하다면 직접 실험하고 확장합니다.
```

---

#### Intro

```
네모:ON의 작업은 두 개의 축으로 나뉩니다.

고객의 브랜드를 실제 비즈니스 구조로 구현하는 **Studio**,
우리 스스로 브랜드가 되어 가능성을 실험하는 **Lab.**

두 축의 역할은 다르지만, 움직이는 방식은 같습니다.
```

---

#### What we do

```
브랜드가 가진 고유한 결을 읽고,
명확한 언어로 설계합니다.
그 본질을 디지털 경험으로 구현합니다.

**담고, 닮고, 작동하다.**

즉, 브랜드의 철학과 분위기를 구조 안에 담아내고,
차가운 디지털 코드가 그 감각을 닮아가도록 만듭니다.
전하고 싶은 가치와 철학이
온라인 안에서 흐르고 연결되어 스스로 작동하도록 합니다.

**Studio**는 선명한 언어와 디지털 구조로 당신의 브랜드를 켭니다.
**Lab.**은 새로운 도구와 실험으로 네모:ON의 브랜드를 증명합니다.
```

---

#### Offering 01 — Studio

##### 네모:ON Studio

```
브랜드를 **보이게** 하고, **이해되게** 만드는 공간입니다.

네모:ON Studio는
클라이언트의 브랜드 철학을 언어와 시각으로 정리하고,
웹사이트, 앱, 자동화 구조까지 연결하는
고객 의뢰형 브랜딩 스튜디오입니다.

단순한 디자인이 아니라,
브랜드의 핵심 철학에서 시작해
실제 사용되는 디지털 구조까지 설계합니다.

이런 작업을 합니다
브랜드 철학 설계 / 디자인시스템 구축 & 로고 / 웹사이트 구축 / 자동화 마케팅 퍼널
```

**CTA**: [Studio 자세히 보기 →]

---

#### Offering 02 — Lab.

##### 네모:ON Lab.

```
브랜드를 **작동하게** 만드는 실험실입니다.

네모:ON Lab.은
외부 의뢰 없이 자체적으로 기획하고 만들고 실행하는
실험 기반 레이블입니다.

우리는 브랜드를 설계하는 데서 끝나지 않고,
스스로 그 브랜드가 되어
도구와 서비스, 인터페이스와 콘텐츠를 통해
그 가능성이 현실에서 어떻게 움직일 수 있는지 실험합니다.
```

**CTA**: [Lab. 자세히 보기 →]

---

#### Closing

```
보여지는 브랜드만으로는 충분하지 않습니다.
작동하는 브랜드가 되어야 합니다.

네모:ON은
Studio에서 브랜드를 구현하고,
Lab.에서 그 가능성을 계속 확장하고 실험합니다.
```

---

### /Offerings/Studio

**목적**: 실제 문의와 전환이 일어나는 페이지.
**구조**: 문제 공감 → 해결 방식 → 제공 범위 → 차별점 → 대상 고객 → 결과 → CTA
**배경**: 크림 (`#f7f1e9`)

#### 섹션 구조

```
Hero → Problem → What We Do → Included → Difference → Why It Matters → Who We Serve → Result → CTA
```

---

#### Hero

```
**당신의 브랜드는 지금, 설명 가능한가요?**

네모:ON Studio는
아이디어와 감각에 머물러 있는 브랜드를
고객이 이해하고 신뢰할 수 있는 구조로 번역하는
실행형 브랜딩 스튜디오입니다.

브랜드의 철학을 정리하고,
고객이 이해할 수 있는 언어와 구조로 바꾸어,
실제 비즈니스 안에서 작동하게 만듭니다.
```

---

#### Problem

```
**이런 고민이 있다면**

아이디어는 있는데
무엇부터 정리해야 할지 막막하다면,

브랜드를 만들긴 했지만
한 문장으로 설명하기 어렵다면,

홈페이지는 있는데
고객이 왜 나를 선택해야 하는지 잘 전달되지 않는다면,

오프라인에서는 어느 정도 자리 잡았지만
디지털 환경에서 내 브랜드를 어떻게 보여줄지 고민이라면,

브랜드는 이미 있지만
이제는 더 잘 작동하는 구조와 시스템으로 정리할 시점이라면,

지금 필요한 건 더 많은 장식이 아니라
더 명확한 구조일 수 있습니다.
```

---

#### What We Do

```
**네모:ON Studio는 무엇을 하나요**

우리는 브랜드의 핵심 철학에서 출발해
실사용 가능한 디지털 구조까지 연결합니다.

브랜드의 **핵심 철학 → 언어화 → 시각화 → 디지털 구현**까지,
아이디어 단계의 브랜드를
하나의 일관된 기준 아래
실제로 사용 가능한 브랜드 구조로 바꾸는 통합 설계 서비스입니다.
```

---

#### Included

```
**제공 범위**

1. 브랜드 전략 설계
   고객 정의, 가치 제안, 차별화 포인트를 정리합니다.

2. 브랜드 철학 수립
   미션, 비전, 브랜드 스토리, 핵심 메시지를 설계합니다.

3. 디자인 시각화
   로고, 컬러, 타이포, 그래픽 모티브를 정리해
   브랜드가 일관된 인상을 갖도록 만듭니다.

4. 웹사이트 구축
   반응형 웹, 예약/결제 구조, SEO,
   확장 가능한 코드 기반의 웹 경험을 구현합니다.

5. 자동화 시스템
   고객 응대, 이메일/문자, 운영 흐름을 정리해
   브랜드가 스스로 24시간 작동할 수 있도록 돕습니다.

6. 세일즈 퍼널 구조
   광고에서 웹, 문의와 전환까지
   흐름이 끊기지 않도록 설계합니다.
```

---

#### Difference

```
**무엇이 다른가요**

우리는 단순 제작자가 아니라 **브랜드 번역가**입니다.

당신의 언어를 고객의 언어로,
당신의 생각을 고객의 공감으로,
당신의 가치를 선택의 이유로 번역합니다.

또한 우리는
브랜드의 본질을 언어로 설계하는 **Language Architect**이자,
그 분위기를 디지털 경험으로 구현하는 **Vibe Coder**입니다.

네모:ON Studio는
감성과 구조를 따로 다루지 않습니다.
구조가 감정을 해치지 않게 만들고,
감성이 구조를 흐리지 않게 만듭니다.

감성 위에 구조를 더해
단단한 결을 함께 만듭니다.
```

---

#### Why It Matters

```
**왜 이 방식이 중요한가요**

브랜딩, 웹, 자동화를 따로 맡기면
톤은 흔들리고, 메시지는 분산되고,
결국 브랜드는 하나처럼 느껴지지 않습니다.

네모:ON Studio는
한 번의 기획으로
브랜드의 언어, 시각, 구조를 함께 설계합니다.

더 일관되고,
더 선명하고,
더 적은 결정으로,
더 빠르게 실행할 수 있습니다.

노코드에 갇히지 않는 실제 코드 기반으로
웹 기능을 확장하거나 구조를 바꿔야 할 때도
브랜드의 가치가 훼손되지 않습니다.

당신의 브랜드는 템플릿이 아닙니다.
```

---

#### Who We Serve

#### 이런 분들과 잘 맞습니다

브랜드를 처음 만드는 단계부터  
기존 브랜드를 더 선명하고 작동 가능하게 정리해야 하는 단계까지,  
각기 다른 전환의 시점에 있는 분들과 잘 맞습니다.

특히 이런 분들에게 적합합니다.

- **예비창업가**  
   **아이디어 → 브랜드**

  아이디어는 있지만  
  무엇부터 어떻게 정리해야 할지 막막한 분.  
  흐릿한 생각을 브랜드 언어로 정리하고,  
  처음 시장에 나가기 위한 구조와 방향을 함께 설계합니다.

  **함께 만들어가는 것들**
  - 사업 아이디어의 브랜드화
  - 초기 브랜딩 전략 수립
  - MVP 브랜드 구축
  - 온라인 진출 로드맵

- **성장모색 대표님**  
   **오프라인 → 온라인**

  오프라인에서 이미 다진 기반을 바탕으로
  이제는 온라인에서도 브랜드가 더 선명하게 작동하길 원하는 대표님.  
  기존에 쌓아온 신뢰와 강점을 디지털 환경에 맞게 재정리하고,  
  온라인에서의 사용자 경험과 브랜드 전달력을 함께 강화합니다.

  **함께 만들어가는 것들**
  - 기존 브랜드 리뉴얼
  - 온라인 브랜딩 강화
  - 웹/앱 사용자 경험 개선
  - 디지털 전환 전략

- **확장을 준비하는 대표님**  
   **브랜드 → 시스템**

  브랜드의 방향은 이미 있지만,  
  이제는 더 효율적으로 작동하는 구조와 시스템이 필요한 대표님.  
  리뉴얼과 자동화, 운영 흐름 정리를 통해  
  브랜드가 사람의 손에만 의존하지 않고 지속적으로 작동할 수 있도록 돕습니다.

  **함께 만들어가는 것들**
  - 브랜드 리뉴얼 방향 정리
  - 운영 흐름 및 구조 재설계
  - 자동화 시스템 도입
  - 확장을 위한 디지털 기반 정비

- **개인브랜드 & 전문가**  
   **전문성 → 브랜드**

  자신만의 철학과 전문성은 분명하지만,  
  그것을 온라인에서 어떻게 보여줘야 할지 고민하는 분.  
  당신의 지식과 경험을 차별화된 브랜드로 정리하고,  
  전문가로서의 존재감이 자연스럽게 드러나는 구조를 함께 만듭니다.

  **함께 만들어가는 것들**
  - 개인 브랜드 아이덴티티 정리
  - 전문성 시각화
  - 개인 웹사이트 구축
  - 콘텐츠 브랜딩 전략

- **스타트업 팀**  
   **MVP → 브랜드**

  서비스나 MVP는 준비되어 있지만,  
  브랜드의 설명력과 확장성이 아직 부족한 팀.  
  고객과 투자자 모두가 이해할 수 있는 브랜드 구조를 정리하고,  
  다음 단계의 성장에 필요한 신뢰와 전달력을 함께 설계합니다.

  **함께 만들어가는 것들**
  - MVP 이후 브랜드 구조 정리
  - 투자자/고객 커뮤니케이션 정비
  - 서비스 메시지 구조화
  - 확장을 위한 브랜드 시스템 설계

---

#### Result

```
**네모:ON Studio와 함께하면**

당신의 브랜드는
그럴듯하게 보이는 수준을 넘어
설명할 수 있고, 보여줄 수 있고, 작동할 수 있는 상태가 됩니다.

무엇부터 해야 할지 모르겠는 막막함 대신
한 번의 결정과 명확한 방향이 생기고,
브랜드는 판매용 계정이 아니라
시간이 쌓일수록 강해지는 자산이 됩니다.
```

---

#### CTA

```
브랜드로 시작하고, 브랜드로 성장하는
모든 대표님의 첫 번째 파트너가 되겠습니다.

제대로 당신만의 브랜드를 켜고 싶다면.

네모:ON Studio와 함께
당신의 브랜드를 설명 가능한 구조로 바꿔보세요.

브랜드를 켤 준비가 되셨나요?
```

**버튼**: [문의하기] [진단 먼저 받아보기]

---

### /Offerings/Lab

**목적**: 네모:ON의 실험 정신과 자체 프로젝트를 보여주는 페이지. 브랜드의 미래, 실험, 확장성**을 보여주는 톤
**배경**: 크림 (`#f7f1e9`) 또는 다크 — 개발시 확정
**상태\*\*: 추후 업데이트 지금은 틀 위주

#### 섹션 구조

```
Hero → Intro → Philosophy → What We Explore → Future → Closing
```

---

#### Hero

```
**우리는 브랜드를 설계하고,
스스로 그 브랜드가 되어 실험합니다.**

네모:ON Lab.은
브랜드 철학을 도구와 제품, 인터페이스와 콘텐츠로 확장하며
브랜드가 실제로 작동하는 순간을 만드는 실험실입니다.
```

---

#### Intro

```
모든 브랜드는 말로만 존재할 수 없습니다.
어느 순간에는 직접 작동해야 합니다.

네모:ON Lab.은
감성과 구조의 철학을 바탕으로
디지털 제품, 웹서비스, 인터페이스 실험을 통해
브랜드의 가능성을 현실로 테스트하는 공간입니다.

네모:ON이 직접 기획하고 개발하여 런칭합니다.
```

---

#### Philosophy

```
**왜 Lab.이 필요한가**

Studio가 고객의 브랜드를 구현하는 곳이라면,
Lab.은 네모:ON이 스스로 질문하고 실험하는 곳입니다.

우리는 브랜드를 설계하는 팀이자,
동시에 그 브랜드가 실제 세상에서 어떻게 작동하는지
직접 만들어보는 팀이기도 합니다.

때로는 분석 도구로,
때로는 자기표현을 돕는 서비스로,
때로는 작은 감정의 결을 움직이는 인터랙션으로,
우리는 철학을 기술로 바꿉니다.
```

---

#### What We Explore

```
**Lab.에서 실험하는 것들**

- 자기탐색 기반 SaaS
- 감정과 사운드를 연결하는 인터페이스
- 브랜딩 셀프 진단 툴

완성된 정답으로 끝내지 않고,
브랜드가 사람과 어떻게 연결되고 작동할 수 있는지에 대한
끊임없는 네모:ON의 질문이자 실험입니다.

점차 더 많은 것들을 순차적으로 실험하게 될 것입니다.
```

> 구체적인 프로젝트 이름은 공개 시점에 추가

---

#### Future

```
**앞으로 열릴 것들**

네모:ON Lab.은 앞으로
Tools, Insight, Prototype, Experiment 형태로 확장됩니다.

어떤 것은 실제 서비스가 되고,
어떤 것은 진단 도구가 되고,
어떤 것은 콘텐츠와 기록으로 남을 것입니다.
```

---

#### Closing

```
브랜드는 살아 있어야 합니다.
네모:ON Lab.은
브랜드를 생각하는 데서 멈추지 않고,
직접 움직이게 만드는 쪽을 택합니다.
```

**CTA**: [소식 받기]

---

### /Diagnosis (브랜드 진단)

**배경**: 크림 (`#f7f1e9`)
**디자인**: 홈화면 네모:ON 언어 기준으로 디자인 (둥근 카드→각진)

#### 진단 구조

2단계 스텝. 질문/선택 로직 기존 그대로 유지.

##### **헤더**

```
당신의 브랜드는 지금 설명 가능한가요?
간단한 진단으로 현재 브랜드 상태를 확인해보세요.
프로그레스 바 (진행률 %)
```

##### 브랜드 정보 체크

**1단계: 지금 브랜드 상태를 알려주세요**

Q1. 어떤 분야의 비즈니스인가요? (단일선택)

```
테크/IT / 헬스케어 / 교육 / 리테일/이커머스
음식/F&B / 패션/뷰티 / 부동산 / 금융 / 기타
```

Q2. 당신의 브랜드는 어떤 느낌으로 기억되길 바라나요? (다중선택)

```
신뢰할 수 있는 / 혁신적인 / 친근한 / 전문적인
럭셔리한 / 심플한 / 역동적인 / 안정적인
```

Q3. 당신의 브랜드와 가장 잘 어울리는 색상 계열은 무엇인가요? (단일선택)

```
블루 계열 (신뢰, 안정) / 그린 계열 (자연, 성장)
레드 계열 (열정, 에너지) / 오렌지 계열 (활동, 따뜻함)
퍼플 계열 (프리미엄, 창의) / 블랙/그레이 계열 (모던, 심플)
기타/잘 모르겠음
```

**2단계: 지금 가장 고민되는 지점을 알려주세요**

Q4. 지금 당신의 브랜드는 어느 단계에 있나요? (단일선택) ← 결과 분류 기준: 결과 로직 이 질문의 답으로 유형 분류

```
브랜드가 전혀 없음 (→ 신생 브랜드형)
로고만 있음 (→ 신생 브랜드형)
기본적인 아이덴티티 있음 (→ 성장 브랜드형)
어느 정도 완성되었지만 개선 필요 (→ 성장 브랜드형)
완성도 높지만 차별화 필요 (→ 완성 브랜드형)
```

Q5. 가장 큰 고민은 무엇인가요? (다중선택)

```
브랜드 정체성이 불분명함
경쟁사와 차별화 어려움
타겟 고객에게 어필하지 못함
일관성 있는 브랜드 경험 부족
온라인에서의 브랜드 인지도 부족
브랜딩 예산 대비 효과 미흡
```

Q6. 지금 고객들은 당신의 브랜드를 어떻게 받아들이고 있나요? (단일선택)

```
아직 시작 전이에요
브랜드를 잘 기억하지 못함
비슷한 경쟁사와 헷갈려함
서비스는 좋다고 하지만 브랜드 임팩트 부족
일부는 좋아하지만 일관성이 없음
대체로 긍정적이지만 더 강한 인상 필요
```

Q7. 이 문제를 해결하는 데 쓸 수 있는 여유는 어느 정도인가요? (단일선택)

```
당장 시작할 준비가 되어있다
3개월 안에 움직일 수 있다
방향만 잡히면 바로 실행 가능하다
아직 탐색 중이다
솔직히 지쳐있다. 그래도 뭔가 바꾸고 싶다
```

#### 결과 화면

```
진단이 완료되었습니다.
지금 당신의 브랜드 유형이 분석되었습니다

[유형 타이틀]
[유형 설명]
주요 개선 방향 (4개)

[결 스캔 — BASIC으로 이어서 보기]
[다시 진단하기]
```

##### **결과 유형 3개**

**신생 브랜드형**
설명: 기본기부터 차근차근 쌓아올려야 하는 단계입니다.
주요 개선 방향:

```
-브랜드 아이덴티티 구축이 최우선
-명확한 타겟 고객 정의 필요
-핵심 메시지 개발 집중
-일관성 있는 비주얼 시스템 구축
```

**성장 브랜드형**
설명: 기본은 갖췄지만 차별화와 강화가 필요한 단계입니다.
주요 개선 방향:

```
-브랜드 포지셔닝 명확화
-경쟁사 대비 차별화 전략
-브랜드 경험 일관성 강화
-디지털 브랜딩 확장
```

**완성 브랜드형**
설명: 기본기는 탄탄하지만 혁신과 진화가 필요한 단계입니다.
주요 개선 방향:

```
-브랜드 리뉴얼 또는 리포지셔닝
-새로운 시장/고객층 공략
-브랜드 확장 전략
-AI 활용 브랜딩 자동화
```

**분류 로직**: Q4 유저 대답 기준

```
"브랜드가 전혀 없음" or "로고만 있음" → 신생 브랜드형
"완성도 높지만 차별화 필요" → 완성 브랜드형
나머지 → 성장 브랜드형
```

#### 결 스캔 모달

"결 스캔 — BASIC으로 이어서 보기" 클릭 시 모달 등장:

```
결 스캔 — BASIC은 코드가 있어야 열려요.
코드는 무료 웨비나 신청자 전용으로 제공됩니다.
코드가 있으면 바로 시작돼요.

[닫기]  [결 스캔으로 이동]

코드가 없나요? 무료 웨비나 신청하기 →  (텍스트 링크 — 웨비나 랜딩페이지 연결)
```

- 결 스캔 링크: 추후 연결 (현재 미완성)
- 웨비나 신청 링크: https://nemo-landing.netlify.app (새 탭에서 열림)

#### 반응형

세로 스택형 — PC 2열 보기가 모바일 1열로 쌓이는 방식

---

### /Contact

**배경**: 크림 (`#f7f1e9`)

#### 콘텐츠 내용/레이아웃

```
[좌] 빅 카피
궁금하거나
의뢰하실 사항이 있다면
문의해주세요.

[우] 대화형 폼
안녕하세요.
저는 [회사명을 입력해주세요.(선택)] 의 [이름을 입력해주세요.] 입니다.

[프로젝트 의뢰] [웨비나 관련 문의] [채용 / 협업 문의] [기타 문의]  ← 선택형 버튼
를 하고 싶습니다.

문의 내용은 아래와 같습니다.
[내용을 입력해주세요.]

[참고사이트 주소를 입력해주세요.(선택)]

↓ 파일 첨부     관련 파일을 첨부합니다.
(최대 40MB, jpg/jpeg/png/pdf/zip 등)

답변은 [전화번호('-' 포함)를 입력해주세요.] , [이메일 주소를 입력해주세요.] 로 받고 싶어요.

□ 개인정보처리방침에 동의합니다. (필수)

[문의하기]
```

#### 제출 후 안내문

**문의가 전달되었습니다.**
남겨주신 내용을 확인 후 입력해주신 연락처로 답변드리겠습니다.

#### 폼 전송

- Resend (Next.js API route) → 단테 이메일로 직접 전송
- 스팸 차단: Honeypot 방식 (숨겨진 필드, UX 영향 없음)

#### 반응형

PC: 좌우 2단 (좌: 빅카피 / 우: 폼)
태블릿/모바일: 상하 전환 (상: 빅카피 / 하: 폼)

참고사이트 | EASYMEDIA (https://www.easymedia.net/kr/inquiry)

---

## 100. PENDING 항목

**히어로**

- [ ] 반응형 레이아웃 세부 확정 (개발시)
- [ ] 폰트 사이즈 및 레이아웃 에디토리얼 그리드 기준 재정리
- [ ] 컴포넌트 구조 재설계 (가로스크롤 제거 후)

**공통**

- [ ] 폰트별 섹션 적용 기준 및 사이즈 체계
- [ ] 그리드 PC 기준 컬럼 수 확정
- [ ] 라이트 배경색 최종 결정 (`#f7f1e9` vs `#faf7f2`)
- [ ] 브랜드 스토리 — 더 알아보기 (about 링크) 추가

**섹션별**

- [ ] Pain & 공명 — 네모 왼쪽 위치 수치
- [x] Pain & 공명 — 반응형 레이아웃
- [x] 메시지 — 반응형 레이아웃
- [ ] ForWho — 카드별 텍스트 위치 (개발시 확정)
- [x] ForWho — 반응형 레이아웃
- [x] 브랜드 스토리 — 반응형 레이아웃

**나머지 페이지**

- [x] /about — 기획 확정
- [x] /offerings — 기획 확정
- [x] /offerings/studio — 기획 확정
- [x] /offerings/lab — 기획 확정 (추후 공개)
- [x] /diagnosis — 기획 확정
- [x] /contact — 기획 확정
- [ ] /showcase (추후 공개)
- [ ] /story (추후 공개)

---
